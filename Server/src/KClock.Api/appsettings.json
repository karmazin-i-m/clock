{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "_comment": "Never filled in here. Set via ConnectionStrings__App / ConnectionStrings__Ingest environment variables (see .env.example) — these connect as clock_app and clock_ingest respectively, never as the superuser or clock_migrator.",
    "App": "",
    "Ingest": ""
  },
  "Authentication": {
    "Google": {
      "_comment": "Set via user-secrets in development, or GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET env vars in compose. Never committed here."
    }
  }
}
